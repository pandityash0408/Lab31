version: '3.8'

services:

  backend:
    build: ./backend
    container_name: lab27-backend
    ports:
      - "5002:5002"
    environment:
      MONGO_URI: mongodb+srv://DBUser:7RQTUWIxHvqC8OnX@cluster01.voocjde.mongodb.net/?appName=Cluster01

  frontend:
    build: ./frontend
    container_name: lab27-frontend
    ports:
      - "3002:3002"
    environment:
      PORT: 3002
    depends_on:
      - backend